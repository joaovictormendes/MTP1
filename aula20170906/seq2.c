#include <conio.h>
#include <stdio.h>

void main ()
{
	float b, h, area;
	printf("Digite a base do triangulo: ");
	scanf(%f, &b);
	printf("\nDigite a altura do triangulo: ");
	scanf(%f, &h);
	area = (b*h)/2;
	printf("\nArea: %d\n", area);
	getche();
	return 0;
}
