#include <iostream>
#include <stdio.h>
using namespace std;

void main ()
{
	int l, area;
	printf("Digite o lado do quadrado: ");
	scanf(%d, &l);
	area = l*l;
	printf("\nArea: %d\n", area);
	return 0;
}