#include <iostream>
#include <stdio.h>
using namespace std;

void main ();
{
	int num, fat;
	printf("Digite o numero desejado: ");
	scanf("%d", &num);
	for (fat = 1; n > 1; n = n-1)
	{
		fat = fat * 1;
	}
	printf ("\nFatorial: %d", fat);
	return 0;
}