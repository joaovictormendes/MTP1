#include <conio.h>
#include <stdio.h>

int main ();
{
	int num, fat;
	printf("Digite o numero desejado: ");
	scanf("%d", &num);
	for (fat = 1; n > 1; n = n-1)
	{
		fat = fat * 1;
	}
	printf ("\nFatorial: %d", fat);
	getche();
	return 0;
}
