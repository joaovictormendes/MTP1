#include <iostream>
#include <stdio.h>
using namespace std;

void main ()
{
	int l, area;
	printf ("Digite o lado do quadrado: \n")
	scanf ("%d", &l);
	area = l*l;
	printf ("A area do quadrado sera: %d", area);
	return 0;
}
